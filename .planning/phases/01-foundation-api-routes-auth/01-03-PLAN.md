---
phase: 01-foundation-api-routes-auth
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - app/error.tsx
  - app/global-error.tsx
  - app/not-found.tsx
  - app/loading.tsx
  - app/dashboard/error.tsx
  - app/dashboard/loading.tsx
  - app/creator/[handle]/error.tsx
  - app/creator/[handle]/loading.tsx
autonomous: false
requirements:
  - INFRA-03
  - AUTH-01
  - AUTH-02
  - AUTH-03

must_haves:
  truths:
    - "App shows error boundary UI with retry button on API failure (not white screen)"
    - "App shows loading skeleton while pages load"
    - "Layout-level errors (PrivyProvider crash) show global error boundary"
    - "404 page shows custom not-found UI"
    - "User can sign in with Google via Privy (already wired, verify works)"
    - "Privy creates embedded Solana wallet on first login (already configured, verify works)"
    - "User session persists across browser refresh (Privy handles, verify works)"
  artifacts:
    - path: "app/error.tsx"
      provides: "Root error boundary with retry"
      min_lines: 15
    - path: "app/global-error.tsx"
      provides: "Layout-level error boundary with own html/body"
      min_lines: 15
    - path: "app/not-found.tsx"
      provides: "Custom 404 page"
      min_lines: 10
    - path: "app/loading.tsx"
      provides: "Root loading state"
      min_lines: 5
    - path: "app/dashboard/error.tsx"
      provides: "Dashboard error boundary"
      min_lines: 15
    - path: "app/dashboard/loading.tsx"
      provides: "Dashboard loading state"
      min_lines: 5
    - path: "app/creator/[handle]/error.tsx"
      provides: "Creator page error boundary"
      min_lines: 15
    - path: "app/creator/[handle]/loading.tsx"
      provides: "Creator page loading state"
      min_lines: 5
  key_links:
    - from: "app/error.tsx"
      to: "app/page.tsx"
      via: "Next.js error boundary convention catches page errors"
      pattern: "use client"
    - from: "app/global-error.tsx"
      to: "app/layout.tsx"
      via: "Next.js global-error catches layout errors"
      pattern: "use client"
---

<objective>
Add error boundaries, loading states, not-found page, and verify Privy auth works end-to-end.

Purpose: Error boundaries prevent white screens on failure (INFRA-03). Auth verification confirms Privy config works as expected (AUTH-01/02/03). These are critical for hackathon judging -- a judge hitting an error should see a friendly UI, not a crash.
Output: Error/loading/not-found files in all route segments. Verified auth flow.
</objective>

<execution_context>
@/Users/leonardojaques/.claude/get-shit-done/workflows/execute-plan.md
@/Users/leonardojaques/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-api-routes-auth/01-RESEARCH.md
@.planning/codebase/CONVENTIONS.md

<interfaces>
<!-- Existing layout structure -->
```typescript
// app/layout.tsx (after Plan 02 removes force-dynamic)
export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en" className="dark">
      <body className="bg-background text-foreground antialiased">
        <Providers>{children}</Providers>
      </body>
    </html>
  );
}
```

<!-- Tailwind theme tokens from globals.css -->
```css
@theme {
  --color-background: ...;
  --color-foreground: ...;
  --color-primary: ...;
  --color-primary-foreground: ...;
  --color-muted: ...;
  --color-muted-foreground: ...;
  --color-accent: ...;
  --color-border: ...;
}
```

<!-- Privy hooks available (from research) -->
```typescript
import { usePrivy, useLogin, useLogout } from '@privy-io/react-auth';
import { useSolanaWallets } from '@privy-io/react-auth/solana';

const { authenticated, user, ready } = usePrivy();
const { login } = useLogin({ onComplete: (user) => { /* ... */ } });
const { wallets } = useSolanaWallets();
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create error boundaries, loading states, and not-found page for all route segments</name>
  <files>app/error.tsx, app/global-error.tsx, app/not-found.tsx, app/loading.tsx, app/dashboard/error.tsx, app/dashboard/loading.tsx, app/creator/[handle]/error.tsx, app/creator/[handle]/loading.tsx</files>
  <action>
    1. Create `app/error.tsx` (client component):
       - 'use client' directive at top
       - Receive { error, reset } props (error: Error and { digest?: string }, reset: () => void)
       - Render centered div with: "Something went wrong" heading, error.message in muted text, "Try again" button calling reset()
       - Use Tailwind classes consistent with app: flex min-h-screen flex-col items-center justify-center gap-4
       - Button: rounded-lg bg-primary px-4 py-2 text-primary-foreground hover:bg-primary/90
       - Console.error the error for debugging

    2. Create `app/global-error.tsx` (client component):
       - 'use client' directive
       - Same props as error.tsx
       - MUST render its own html element with lang="en" className="dark" and body tags (replaces entire shell)
       - Body gets className="bg-background text-foreground antialiased"
       - Same UI as error.tsx inside body
       - This catches layout-level errors (e.g., PrivyProvider crash)

    3. Create `app/not-found.tsx`:
       - Render "Page not found" heading
       - Muted description: "The page you're looking for doesn't exist."
       - Link to home: "Go home" link styled as button
       - Import Link from 'next/link'

    4. Create `app/loading.tsx`:
       - Simple centered loading UI: spinning indicator + "Loading..." text
       - Use animate-spin on a simple div or SVG circle
       - Minimal: this is a suspense fallback

    5. Create `app/dashboard/error.tsx`:
       - Same pattern as root error.tsx
       - Customize heading: "Dashboard error"

    6. Create `app/dashboard/loading.tsx`:
       - "Loading dashboard..." centered text with spinner

    7. Create `app/creator/[handle]/error.tsx`:
       - Same pattern as root error.tsx
       - Customize heading: "Failed to load creator"

    8. Create `app/creator/[handle]/loading.tsx`:
       - "Loading creator..." centered text with spinner

    All error.tsx files are 'use client' components (required by Next.js).
    All loading.tsx files are server components (no 'use client' needed).
    Use consistent Tailwind styling matching existing dark theme.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>8 files created: error.tsx (root + dashboard + creator), global-error.tsx, not-found.tsx, loading.tsx (root + dashboard + creator). All type-check. Error boundaries show retry button. Loading states show spinner.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete Phase 1 foundation end-to-end</name>
  <files>N/A</files>
  <action>
    Human verifies the complete Phase 1 foundation works:
    1. Start dev server: `npm run dev`
    2. Visit http://localhost:3000 -- should load without errors
    3. Visit http://localhost:3000/dashboard -- should redirect to / (no privy-token cookie)
    4. Visit http://localhost:3000/api/creators -- should return JSON data (or 502 if no BAGS_API_KEY)
    5. Visit http://localhost:3000/api/creator/elonmusk -- should return JSON data (or 502)
    6. Visit http://localhost:3000/nonexistent -- should show custom 404 page
    7. Click Google login on home page -- should open Privy modal (AUTH-01)
    8. After login, check browser console for embedded wallet (AUTH-02)
    9. After login, refresh page -- should still be logged in (AUTH-03)
    10. After login, visit /dashboard -- should NOT redirect (has cookie now)
  </action>
  <verify>All 10 checks pass. If BAGS_API_KEY is not set, API routes return 502 with error message (not crash). If set, they return real Bags data.</verify>
  <done>Human confirms all checks pass by typing "approved".</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- `npx vitest run` passes (all tests from Plan 01 and Plan 02 still green)
- Dev server starts without errors
- Visiting /nonexistent shows custom 404
- Error boundary renders on simulated error
</verification>

<success_criteria>
- Error boundaries in all route segments (root, dashboard, creator)
- Global error boundary catches layout-level crashes
- Custom 404 page
- Loading states in all route segments
- Privy Google login works (AUTH-01)
- Embedded Solana wallet created on login (AUTH-02)
- Session persists across refresh (AUTH-03)
- Human verification confirms all 10 checks pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-api-routes-auth/01-03-SUMMARY.md`
</output>
