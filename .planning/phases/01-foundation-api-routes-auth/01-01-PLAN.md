---
phase: 01-foundation-api-routes-auth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - vitest.config.ts
  - lib/env.ts
  - lib/cache.ts
  - instrumentation.ts
  - tests/lib/env.test.ts
  - tests/lib/cache.test.ts
  - package.json
autonomous: true
requirements:
  - INFRA-04
  - INFRA-02

must_haves:
  truths:
    - "Missing env var crashes at startup with clear message listing which vars are missing"
    - "Cached responses return within 1ms on repeat calls within TTL"
    - "Cache entries expire after TTL and return null"
    - "All tests pass via npx vitest run"
  artifacts:
    - path: "lib/env.ts"
      provides: "Zod env schema + validateEnv function"
      exports: ["validateEnv", "Env"]
    - path: "lib/cache.ts"
      provides: "In-memory TTL cache utility"
      exports: ["cache"]
    - path: "instrumentation.ts"
      provides: "Next.js startup hook calling validateEnv"
      exports: ["register"]
    - path: "vitest.config.ts"
      provides: "Vitest config with path aliases"
    - path: "tests/lib/env.test.ts"
      provides: "Env validation tests"
    - path: "tests/lib/cache.test.ts"
      provides: "Cache TTL tests"
  key_links:
    - from: "instrumentation.ts"
      to: "lib/env.ts"
      via: "dynamic import of validateEnv"
      pattern: "import.*@/lib/env"
    - from: "lib/cache.ts"
      to: "API route handlers (Plan 02)"
      via: "cache.get/cache.set calls"
      pattern: "cache\\.(get|set)"
---

<objective>
Install test framework, create env validation and caching utilities, and verify with unit tests.

Purpose: These are the foundation utilities that all subsequent Phase 1 plans depend on. Env validation catches misconfig before first request. Cache reduces Bags API calls.
Output: Working vitest setup, lib/env.ts, lib/cache.ts, instrumentation.ts, all with passing tests.
</objective>

<execution_context>
@/Users/leonardojaques/.claude/get-shit-done/workflows/execute-plan.md
@/Users/leonardojaques/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-api-routes-auth/01-RESEARCH.md
@.planning/codebase/CONVENTIONS.md

<interfaces>
<!-- Existing codebase patterns the executor needs -->

From lib/bags.ts (singleton pattern to follow):
```typescript
let sdk: BagsSDK | null = null;
export function getBagsSDK(): BagsSDK {
  if (!sdk) {
    const apiKey = process.env.BAGS_API_KEY;
    if (!apiKey) throw new Error("BAGS_API_KEY is not set");
    sdk = new BagsSDK(apiKey, getHeliusConnection());
  }
  return sdk;
}
```

From .env.example (env vars to validate):
```
NEXT_PUBLIC_PRIVY_APP_ID=
PRIVY_APP_SECRET=
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
HELIUS_API_KEY=
NEXT_PUBLIC_HELIUS_RPC_URL=
BAGS_API_KEY=
```

From tsconfig.json (path alias for vitest config):
```json
{ "paths": { "@/*": ["./*"] } }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install dependencies and create vitest config, env validation, and cache utility with tests</name>
  <files>package.json, vitest.config.ts, lib/env.ts, lib/cache.ts, instrumentation.ts, tests/lib/env.test.ts, tests/lib/cache.test.ts</files>
  <behavior>
    - env.test: validateEnv() throws with descriptive message when BAGS_API_KEY is missing
    - env.test: validateEnv() throws listing ALL missing vars, not just the first one
    - env.test: validateEnv() returns typed Env object when all vars present
    - env.test: validateEnv() rejects NEXT_PUBLIC_SUPABASE_URL that is not a valid URL
    - cache.test: cache.get() returns null for non-existent key
    - cache.test: cache.set() then cache.get() returns the stored value
    - cache.test: cache.get() returns null after TTL expires (use fake timers)
    - cache.test: cache.clear() removes all entries
    - cache.test: cache.get() returns different types (object, array, string)
  </behavior>
  <action>
    1. Install dependencies: `npm install @privy-io/node zod && npm install -D vitest @vitejs/plugin-react`

    2. Create `vitest.config.ts`:
       - Import `defineConfig` from vitest/config
       - Set resolve alias: `@` maps to project root (match tsconfig paths)
       - No React plugin needed (testing pure utilities, not components)

    3. Create `lib/env.ts`:
       - Import z from 'zod'
       - Define envSchema with z.object for all 8 env vars from .env.example
       - BAGS_API_KEY, PRIVY_APP_SECRET, HELIUS_API_KEY: z.string().min(1)
       - NEXT_PUBLIC_PRIVY_APP_ID, NEXT_PUBLIC_SUPABASE_ANON_KEY: z.string().min(1)
       - NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_HELIUS_RPC_URL: z.string().url()
       - SUPABASE_SERVICE_ROLE_KEY: z.string().min(1)
       - Export type Env = z.infer<typeof envSchema>
       - Export function validateEnv(): Env that uses safeParse on process.env
       - On failure: throw Error with formatted list of ALL missing/invalid vars
       - On success: return result.data

    4. Create `instrumentation.ts`:
       - Export async function register() that dynamically imports and calls validateEnv
       - Pattern: `const { validateEnv } = await import('@/lib/env'); validateEnv();`

    5. Create `lib/cache.ts`:
       - Type CacheEntry<T> = { data: T; expiresAt: number }
       - Module-level const store = new Map<string, CacheEntry<unknown>>()
       - Export const cache with get<T>(key): T | null, set<T>(key, data, ttlMs): void, clear(): void
       - get: check if entry exists, if Date.now() > expiresAt delete and return null, else return data
       - set: store.set with expiresAt = Date.now() + ttlMs
       - clear: store.clear()

    6. Write tests following behavior spec above. Use vi.useFakeTimers() for TTL expiry test, vi.advanceTimersByTime() to simulate passage of time. Restore real timers in afterEach.
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose</automated>
  </verify>
  <done>All env and cache tests pass. vitest config resolves @/ alias. instrumentation.ts exists and calls validateEnv on register().</done>
</task>

</tasks>

<verification>
- `npx vitest run` exits 0 with all tests passing
- `npx tsc --noEmit` passes (type check)
</verification>

<success_criteria>
- lib/env.ts validates all 8 env vars with Zod, throws descriptive error on failure
- lib/cache.ts provides get/set/clear with TTL expiration
- instrumentation.ts calls validateEnv on startup
- vitest configured with path aliases, all tests green
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-api-routes-auth/01-01-SUMMARY.md`
</output>
