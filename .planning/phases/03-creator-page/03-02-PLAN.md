---
phase: 03-creator-page
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - app/creator/[handle]/page.tsx
  - app/creator/[handle]/loading.tsx
  - components/creator-profile.tsx
  - components/token-stats.tsx
  - components/fee-earnings.tsx
  - components/status-badge.tsx
  - components/trade-buttons.tsx
  - components/launch-prompt.tsx
autonomous: false
requirements:
  - CREA-01
  - CREA-02
  - CREA-03
  - CREA-04
  - CREA-05
  - CREA-06

must_haves:
  truths:
    - "Visiting /creator/elonmusk shows avatar, name, handle, status badge, and token stats"
    - "Token stats row shows Price, Market Cap, Volume, Holders with teal values"
    - "Fee Earnings section shows lifetime fees below token stats"
    - "Buy Shares and Sell Shares buttons appear below fee earnings"
    - "Clicking Buy or Sell shows a Coming soon toast"
    - "Visiting a handle with no token shows launch prompt instead of stats"
    - "Launch prompt shows encouraging copy and a purple Launch Token button with (0.2 SOL)"
    - "Clicking Launch Token shows a Coming soon toast"
    - "Back arrow navigates to landing page"
    - "Page layout is centered max-w-2xl with hero banner structure"
    - "Stats collapse to 2x2 grid on mobile"
    - "Loading state shows skeleton pulse matching final layout"
  artifacts:
    - path: "app/creator/[handle]/page.tsx"
      provides: "Server Component composing all creator page sections"
      contains: "getCreatorPageData"
    - path: "components/creator-profile.tsx"
      provides: "Hero section: avatar, name, handle, status badge, back link"
      contains: "CreatorProfile"
    - path: "components/token-stats.tsx"
      provides: "Horizontal stat row with 4 metrics"
      contains: "TokenStats"
    - path: "components/fee-earnings.tsx"
      provides: "Fee earnings section"
      contains: "FeeEarnings"
    - path: "components/status-badge.tsx"
      provides: "Graduation status badge"
      contains: "StatusBadge"
    - path: "components/trade-buttons.tsx"
      provides: "Buy/Sell buttons with Coming soon toast"
      contains: "TradeButtons"
    - path: "components/launch-prompt.tsx"
      provides: "No-token state with launch CTA"
      contains: "LaunchPrompt"
    - path: "app/creator/[handle]/loading.tsx"
      provides: "Skeleton loading state matching final layout"
      contains: "CreatorLoading"
  key_links:
    - from: "app/creator/[handle]/page.tsx"
      to: "lib/creators.ts"
      via: "import getCreatorPageData"
      pattern: "getCreatorPageData"
    - from: "app/creator/[handle]/page.tsx"
      to: "components/creator-profile.tsx"
      via: "import CreatorProfile"
      pattern: "CreatorProfile"
    - from: "app/creator/[handle]/page.tsx"
      to: "components/token-stats.tsx"
      via: "import TokenStats"
      pattern: "TokenStats"
    - from: "app/creator/[handle]/page.tsx"
      to: "components/trade-buttons.tsx"
      via: "import TradeButtons"
      pattern: "TradeButtons"
    - from: "app/creator/[handle]/page.tsx"
      to: "components/launch-prompt.tsx"
      via: "import LaunchPrompt"
      pattern: "LaunchPrompt"
---

<objective>
Build the creator page UI: profile hero, token stats, fee earnings, status badge, trade buttons, launch prompt, and loading skeleton.

Purpose: Transform the placeholder creator page into a full profile displaying live token data (or a launch CTA for creators without tokens). This is the core page users visit after clicking a creator card on the landing page.

Output: 6 new components + updated page.tsx + updated loading.tsx, all matching user's locked design decisions.
</objective>

<execution_context>
@/Users/leonardojaques/.claude/get-shit-done/workflows/execute-plan.md
@/Users/leonardojaques/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-creator-page/03-CONTEXT.md
@.planning/phases/03-creator-page/03-RESEARCH.md
@.planning/phases/03-creator-page/03-01-SUMMARY.md

@app/creator/[handle]/page.tsx
@app/creator/[handle]/loading.tsx
@app/creator/[handle]/error.tsx
@components/creator-card.tsx
@app/globals.css

<interfaces>
<!-- From Plan 01 output — CreatorPageData type and formatters -->

From lib/creators.ts (after Plan 01):
```typescript
export interface CreatorPageData {
  profile: {
    username: string;
    displayName: string;
    avatarUrl: string | null;
    wallet: string | null;
  };
  token: {
    mint: string;
    name: string;
    symbol: string;
    icon: string | null;
    price: number | null;
    marketCap: number | null;
    volume: number | null;
    holders: number | null;
    lifetimeFees: string;
    status: "graduated" | "bonding-curve" | "migrating" | "pre-launch";
  } | null;
}

export async function getCreatorPageData(handle: string): Promise<CreatorPageData>;
export function formatPrice(usd: number | null): string;
export function formatMarketCap(mcap: number | null): string;
export function formatVolume(volume: number | null): string;
export function formatHolders(count: number | null): string;
export function formatFees(lamportsStr: string): string;
```

Tailwind semantic colors (from globals.css):
```
--color-background: #0a0a0a;
--color-foreground: #ededed;
--color-primary: #6d28d9;         (purple — Launch button)
--color-primary-foreground: #f5f3ff;
--color-muted: #1a1a2e;
--color-muted-foreground: #a1a1aa; (stat labels)
--color-accent: #14b8a6;          (teal — stat values, Buy button)
--color-border: #27272a;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create all UI components</name>
  <files>components/creator-profile.tsx, components/token-stats.tsx, components/fee-earnings.tsx, components/status-badge.tsx, components/trade-buttons.tsx, components/launch-prompt.tsx</files>
  <action>
    Create 6 components. Only TradeButtons and LaunchPrompt need "use client" (for toast interaction). All others are Server Components (no directive needed).

    **components/status-badge.tsx** (Server Component):
    - Props: `{ status: "graduated" | "bonding-curve" | "migrating" | "pre-launch" }`
    - Renders a small pill/badge with status label and color:
      - graduated: teal bg/text (accent), label "Graduated"
      - bonding-curve: purple bg/text (primary), label "Bonding Curve"
      - migrating: yellow bg/text, label "Migrating"
      - pre-launch: muted bg/text, label "Pre-Launch"
    - Use `bg-{color}/20 text-{color} border border-{color}/30` pattern for each status
    - Small text (text-xs), rounded-full, px-3 py-1

    **components/creator-profile.tsx** (Server Component):
    - Props: `{ profile: CreatorPageData["profile"]; status: CreatorPageData["token"]["status"] | null }`
    - Back arrow/link at top-left: `<Link href="/">` with a left arrow character or SVG, text "Back"
    - Centered avatar: use next/image with `unoptimized` prop for external URLs, fallback to initial letter circle (same pattern as CreatorCard but larger — use w-24 h-24 / 96px)
    - Display name below avatar, large text (text-2xl or text-3xl font-bold)
    - Handle below name in muted-foreground: @{username}
    - StatusBadge beneath handle (only if status is not null)

    **components/token-stats.tsx** (Server Component):
    - Props: `{ token: NonNullable<CreatorPageData["token"]> }`
    - Import formatPrice, formatMarketCap, formatVolume, formatHolders from lib/creators
    - Horizontal stat row: `grid grid-cols-2 gap-4 sm:grid-cols-4` (collapses to 2x2 on mobile per user decision)
    - Each stat: label above (text-sm text-muted-foreground uppercase tracking-wide), value below (text-lg font-semibold text-accent)
    - Four stats: Price, Market Cap, Volume, Holders
    - Wrapped in max-w-2xl, centered with w-full

    **components/fee-earnings.tsx** (Server Component):
    - Props: `{ lifetimeFees: string }`
    - Import formatFees from lib/creators
    - Separate section: label "Fee Earnings" in muted-foreground, value below in larger text
    - Visually distinct from token stats (add a border-t border-border or spacing)
    - Centered layout

    **components/trade-buttons.tsx** ("use client"):
    - Props: `{ handle: string }`
    - Two buttons side by side in a flex container with gap-3
    - "Buy Shares" button: bg-accent text-background, rounded-lg, px-6 py-3, font-semibold
    - "Sell Shares" button: border border-border text-foreground, rounded-lg, px-6 py-3, font-semibold
    - On click: show a minimal toast "Coming soon" that auto-dismisses after 2.5s
    - Toast implementation: useState for toast message, positioned absolute above buttons, bg-muted text-muted-foreground rounded-lg shadow-lg
    - No external toast library — simple inline state (per RESEARCH.md)

    **components/launch-prompt.tsx** ("use client"):
    - Props: `{ handle: string }`
    - Encouraging copy: "No token yet. Be the first to back @{handle}!" in text-lg text-foreground
    - Launch Token button: bg-primary text-primary-foreground, rounded-lg, px-6 py-3, font-semibold
    - Cost hint below or beside button: "(0.2 SOL)" in text-sm text-muted-foreground
    - On click: show same "Coming soon" toast pattern as TradeButtons
    - Section should replace the stats area (same vertical space)

    All components must use Tailwind CSS v4 semantic tokens (not hardcoded hex colors). Follow the existing CreatorCard component as a styling reference for consistency.
  </action>
  <verify>
    <automated>npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>All 6 components created, TypeScript compiles with no errors, consistent styling with project conventions.</done>
</task>

<task type="auto">
  <name>Task 2: Compose creator page and update loading skeleton</name>
  <files>app/creator/[handle]/page.tsx, app/creator/[handle]/loading.tsx</files>
  <action>
    **app/creator/[handle]/page.tsx** — Replace the existing placeholder entirely. This is a Server Component (no "use client"):
    - Import getCreatorPageData from "@/lib/creators"
    - Import CreatorProfile, TokenStats, FeeEarnings, TradeButtons, LaunchPrompt from "@/components/*"
    - Async function: `const { handle } = await params;` then `const data = await getCreatorPageData(handle);`
    - Layout: `<main className="flex min-h-screen flex-col items-center px-4 py-8">`
    - Inner container: `<div className="w-full max-w-2xl">` (per user decision: max-w-2xl centered)
    - Render CreatorProfile with data.profile and data.token?.status
    - Conditional rendering:
      - If data.token exists: TokenStats + FeeEarnings + TradeButtons
      - If data.token is null: LaunchPrompt
    - Add appropriate spacing between sections (mt-8 or similar)
    - Export metadata: `export function generateMetadata` returning title with handle for SEO

    **app/creator/[handle]/loading.tsx** — Update the existing skeleton to match the new page layout:
    - Skeleton pulse for avatar (h-24 w-24 rounded-full)
    - Skeleton for name, handle, badge
    - Skeleton for 4-stat grid (2x2 on mobile, 4 cols on sm+)
    - Skeleton for fee earnings section
    - Skeleton for two buttons
    - All using `animate-pulse` with bg-border placeholders (consistent with existing GridSkeleton pattern)
    - Layout must match the final page structure so transitions are smooth

    Run `npx next build` to verify no compile errors. Then run all tests.
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>Creator page renders full profile for handles with tokens. Creator page renders launch prompt for handles without tokens. Loading skeleton matches final layout. Build succeeds. All tests pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of creator page</name>
  <files>app/creator/[handle]/page.tsx</files>
  <action>Human verifies the complete creator page visually.</action>
  <verify>User approves the visual output.</verify>
  <done>Creator page visually matches locked design decisions and both states work correctly.</done>
  <what-built>Complete creator page with profile hero, token stats, fee earnings, trade buttons, launch prompt (no-token state), and skeleton loading.</what-built>
  <how-to-verify>
    1. Run `npm run dev` and visit http://localhost:3000
    2. Click any creator card on the landing page — should navigate to /creator/[handle]
    3. Verify the creator page shows:
       a. Back arrow/link to landing page (top-left)
       b. Centered avatar (larger than card avatar)
       c. Display name and @handle below avatar
       d. Status badge beneath handle
       e. Token stats row: Price, Market Cap, Volume, Holders (teal values, muted labels)
       f. Fee Earnings section below stats
       g. Buy Shares and Sell Shares buttons below fee earnings
    4. Click "Buy Shares" — should show "Coming soon" toast
    5. Click "Sell Shares" — should show "Coming soon" toast
    6. Visit http://localhost:3000/creator/nonexistent_user_xyz
       a. Should show the no-token state: avatar/handle preserved, launch prompt instead of stats
       b. Launch prompt shows encouraging copy and purple "Launch Token" button with "(0.2 SOL)"
       c. Click "Launch Token" — should show "Coming soon" toast
    7. Resize browser to mobile width — stats should collapse to 2x2 grid
    8. Refresh any creator page — loading skeleton should briefly appear with correct layout
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `npx vitest run --reporter=verbose` — all tests pass
- `npx next build` — build succeeds with no errors
- Visual verification: creator page matches locked design decisions
- Both token and no-token states render correctly
- Mobile responsive layout works
</verification>

<success_criteria>
- Creator page at /creator/[handle] shows live token data (price, market cap, volume, holders)
- Fee earnings section displays lifetime fees
- Status badge shows correct graduation state
- Buy/Sell buttons show "Coming soon" toast
- No-token state shows launch prompt with purple button and (0.2 SOL) hint
- Page works for any Twitter handle typed in URL
- Centered max-w-2xl layout with back navigation
- Stats collapse to 2x2 on mobile
- Loading skeleton matches final layout
- Build succeeds, all tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/03-creator-page/03-02-SUMMARY.md`
</output>
